<% content_for :title, "Réglages de la conversation" %>

<div class="mx-auto mt-10 max-w-md px-4">
  <h1 class="mb-6 text-2xl font-semibold">Réglages de la conversation</h1>
  <%= render "form", conversation: @conversation, url: conversation_path(@conversation) %>
  <p class="mt-6 text-sm"><%= link_to "← Conversation", conversation_path(@conversation), class: "underline" %></p>
</div>
