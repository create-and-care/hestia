<% content_for :title, t(".title") %>

<div class="mx-auto mt-10 max-w-md px-4">
  <h1 class="mb-6 text-2xl font-semibold"><%= t(".title") %></h1>
  <%= render "form", conversation: @conversation, url: conversations_path %>
  <p class="mt-6 text-sm"><%= link_to t(".back_to_messages"), conversations_path, class: "underline" %></p>
</div>
