<% content_for :title, t(".title") %>

<div class="mx-auto mt-10 max-w-3xl px-4">
  <div class="mb-4"><%= module_breadcrumb(:shopping) %></div>

  <header class="mb-6 flex items-center justify-between">
    <h1 class="text-2xl font-semibold"><%= t(".heading") %></h1>
    <%= link_to t(".new_list"), new_shopping_list_path,
          class: "rounded-md bg-gray-900 px-3 py-1.5 text-sm font-medium text-white hover:bg-gray-700" %>
  </header>

  <%= render "shared/flash" %>

  <% if @shopping_lists.any? %>
    <ul class="divide-y divide-gray-100 rounded-lg border border-gray-200">
      <% @shopping_lists.each do |list| %>
        <li class="flex items-center justify-between px-4 py-3">
          <%= link_to shopping_list_path(list), class: "flex items-center gap-2 font-medium hover:underline" do %>
            <span><%= list.icon.presence || lucide_icon("shopping-cart", css_class: "size-4") %></span>
            <span><%= list.name %></span>
          <% end %>
          <span class="text-sm text-gray-500"><%= t(".items_count", count: list.items.where(checked: false).count) %></span>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="rounded-lg border border-dashed border-gray-300 p-6 text-center text-gray-500">
      <%= t(".empty") %>
    </p>
  <% end %>

  <div class="mt-6 flex gap-4 text-sm">
    <%= link_to t(".catalog_link"), products_path, class: "underline" %>
  </div>
</div>
