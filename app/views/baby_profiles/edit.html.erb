<% content_for :title, "Modifier le profil" %>

<div class="mx-auto mt-10 max-w-md px-4">
  <h1 class="mb-6 text-2xl font-semibold">Modifier le profil</h1>
  <%= render "form", baby: @baby, url: baby_profile_path(@baby) %>
  <p class="mt-6 text-sm"><%= link_to "← Fiche", baby_profile_path(@baby), class: "underline" %></p>
</div>
