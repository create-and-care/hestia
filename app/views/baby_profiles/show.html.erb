<% content_for :title, @baby.name %>

<div class="mt-10 px-4">
  <header class="mb-2 flex items-start justify-between">
    <div>
      <h1 class="flex items-center gap-2 text-2xl font-semibold"><%= lucide_icon("baby", css_class: "size-6") %> <%= @baby.name %></h1>
      <% if @baby.born_on %><p class="text-sm text-gray-500"><%= t("baby_profiles.born_on", date: @baby.born_on.strftime("%d/%m/%Y")) %></p><% end %>
    </div>
    <div class="flex gap-2 text-sm">
      <%= link_to t(".edit"), edit_baby_profile_path(@baby), class: "underline" %>
      <%= button_to t(".delete"), baby_profile_path(@baby), method: :delete,
            form: { data: { turbo_confirm: t(".delete_confirm") } }, class: "cursor-pointer text-red-600 underline" %>
    </div>
  </header>

  <p class="mb-6 text-xs text-gray-400"><%= t(".disclaimer") %></p>

  <%= render "shared/flash" %>

  <section class="mb-8">
    <h2 class="mb-2 flex items-center gap-1.5 font-medium"><%= lucide_icon("milk", css_class: "size-5") %> <%= t(".feedings_title") %></h2>
    <ul class="mb-3 divide-y divide-gray-100 rounded-lg border border-gray-200">
      <% @baby.feeding_sessions.recent.limit(20).each do |session| %>
        <li class="flex items-center justify-between px-3 py-2 text-sm">
          <span>
            <%= feeding_kind_label(session.kind) %>
            <% if session.started_at %><span class="text-gray-400">· <%= session.started_at.strftime("%d/%m %H:%M") %></span><% end %>
            <% if session.duration_minutes %><span class="text-gray-400">· <%= t(".duration_minutes", count: session.duration_minutes) %></span><% end %>
          </span>
          <%= button_to baby_profile_feeding_session_path(@baby, session), method: :delete, class: "cursor-pointer text-gray-400 hover:text-red-600" do %><%= lucide_icon("x", css_class: "size-4") %><% end %>
        </li>
      <% end %>
    </ul>
    <%= form_with model: @feeding_session, url: baby_profile_feeding_sessions_path(@baby), class: "flex flex-wrap items-end gap-2" do |form| %>
      <%= form.select :kind, feeding_kind_options, {}, class: "rounded-md border border-gray-300 px-2 py-1.5 text-sm" %>
      <%= form.datetime_field :started_at, class: "rounded-md border border-gray-300 px-2 py-1.5 text-sm" %>
      <%= form.datetime_field :ended_at, class: "rounded-md border border-gray-300 px-2 py-1.5 text-sm" %>
      <%= form.submit t(".add"), class: "cursor-pointer rounded-md border border-gray-300 px-2 py-1.5 text-sm hover:bg-gray-50" %>
    <% end %>
  </section>

  <section class="mb-8">
    <h2 class="mb-2 flex items-center gap-1.5 font-medium"><%= lucide_icon("carrot", css_class: "size-5") %> <%= t(".food_introductions_title") %></h2>
    <ul class="mb-3 divide-y divide-gray-100 rounded-lg border border-gray-200">
      <% @baby.food_introductions.recent.each do |introduction| %>
        <li class="flex items-center justify-between px-3 py-2 text-sm">
          <span><%= introduction.food %>
            <% if introduction.introduced_on %><span class="text-gray-400">· <%= introduction.introduced_on.strftime("%d/%m/%Y") %></span><% end %>
            <% if introduction.acceptance.present? %><span class="text-gray-400">· <%= introduction.acceptance %></span><% end %>
          </span>
          <%= button_to baby_profile_food_introduction_path(@baby, introduction), method: :delete, class: "cursor-pointer text-gray-400 hover:text-red-600" do %><%= lucide_icon("x", css_class: "size-4") %><% end %>
        </li>
      <% end %>
    </ul>
    <%= form_with model: @food_introduction, url: baby_profile_food_introductions_path(@baby), class: "flex flex-wrap items-end gap-2" do |form| %>
      <%= form.text_field :food, required: true, placeholder: t(".food_placeholder"), class: "flex-1 rounded-md border border-gray-300 px-2 py-1.5 text-sm" %>
      <%= form.date_field :introduced_on, class: "rounded-md border border-gray-300 px-2 py-1.5 text-sm" %>
      <%= form.select :acceptance, t(".acceptance_options"), { include_blank: t(".acceptance_blank") }, class: "rounded-md border border-gray-300 px-2 py-1.5 text-sm" %>
      <%= form.submit t(".add"), class: "cursor-pointer rounded-md border border-gray-300 px-2 py-1.5 text-sm hover:bg-gray-50" %>
    <% end %>
  </section>

  <section class="mb-8">
    <h2 class="mb-2 flex items-center gap-1.5 font-medium"><%= lucide_icon("triangle-alert", css_class: "size-5") %> <%= t(".allergen_tests_title") %></h2>
    <ul class="mb-3 divide-y divide-gray-100 rounded-lg border border-gray-200">
      <% @baby.allergen_tests.recent.each do |test| %>
        <li class="flex items-center justify-between px-3 py-2 text-sm">
          <span><%= test.allergen %>
            <% if test.tested_on %><span class="text-gray-400">· <%= test.tested_on.strftime("%d/%m/%Y") %></span><% end %>
            <% if test.severity.present? %><span class="text-gray-400">· <%= test.severity %></span><% end %>
          </span>
          <%= button_to baby_profile_allergen_test_path(@baby, test), method: :delete, class: "cursor-pointer text-gray-400 hover:text-red-600" do %><%= lucide_icon("x", css_class: "size-4") %><% end %>
        </li>
      <% end %>
    </ul>
    <%= form_with model: @allergen_test, url: baby_profile_allergen_tests_path(@baby), class: "flex flex-wrap items-end gap-2" do |form| %>
      <%= form.text_field :allergen, required: true, placeholder: t(".allergen_placeholder"), class: "flex-1 rounded-md border border-gray-300 px-2 py-1.5 text-sm" %>
      <%= form.date_field :tested_on, class: "rounded-md border border-gray-300 px-2 py-1.5 text-sm" %>
      <%= form.select :severity, t(".severity_options"), { include_blank: t(".severity_blank") }, class: "rounded-md border border-gray-300 px-2 py-1.5 text-sm" %>
      <%= form.submit t(".add"), class: "cursor-pointer rounded-md border border-gray-300 px-2 py-1.5 text-sm hover:bg-gray-50" %>
    <% end %>
  </section>

  <p class="text-sm"><%= link_to t(".back_to_profiles"), baby_profiles_path, class: "underline" %></p>
</div>
