<% content_for :title, "Nouvel animal" %>

<div class="mx-auto mt-10 max-w-xl px-4">
  <h1 class="mb-6 text-2xl font-semibold">Nouvel animal</h1>
  <%= render "form", pet: @pet, url: pets_path %>
  <p class="mt-6 text-sm"><%= link_to "← Animaux", pets_path, class: "underline" %></p>
</div>
