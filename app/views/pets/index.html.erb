<% content_for :title, t(".title") %>
<%= turbo_stream_from Current.household %>

<div class="mt-10 max-w-5xl mx-auto px-4">
  <div class="mb-4"><%= module_breadcrumb(:pets) %></div>

  <header class="mb-6 flex items-center justify-between">
    <h1 class="flex items-center gap-2 text-2xl font-semibold"><%= lucide_icon("paw-print", css_class: "size-6") %> <%= t(".title") %></h1>
    <%= render(Ui::ButtonComponent.new(variant: :default, size: :sm, href: new_pet_path)) { t(".new_pet") } %>
  </header>

  <%= render "shared/flash" %>

  <div id="pets" class="grid gap-3 sm:grid-cols-2">
    <%= render @pets %>
  </div>
  <% if @pets.empty? %>
    <div class="mt-4">
      <%= render(Ui::EmptyComponent.new) do |empty| %>
        <% empty.with_description { t(".empty") } %>
      <% end %>
    </div>
  <% end %>

</div>
