<% content_for :title, "Modifier l'animal" %>

<div class="mx-auto mt-10 max-w-xl px-4">
  <h1 class="mb-6 text-2xl font-semibold">Modifier l'animal</h1>
  <%= render "form", pet: @pet, url: pet_path(@pet) %>
  <p class="mt-6 text-sm"><%= link_to "← Fiche", pet_path(@pet), class: "underline" %></p>
</div>
