<%# The message is added to the thread via the real-time stream; we just reset the field. %>
<%= turbo_stream.replace "new_message" do %>
  <%= render "messages/form", conversation: @conversation, message: @conversation.messages.new %>
<% end %>
