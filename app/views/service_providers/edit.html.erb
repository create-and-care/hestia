<% content_for :title, "Modifier le prestataire" %>

<div class="mx-auto mt-10 max-w-xl px-4">
  <h1 class="mb-6 text-2xl font-semibold">Modifier le prestataire</h1>
  <%= render "form", service_provider: @provider, url: service_provider_path(@provider) %>
  <p class="mt-6 text-sm"><%= link_to "← Prestataires", service_providers_path, class: "underline" %></p>
</div>
