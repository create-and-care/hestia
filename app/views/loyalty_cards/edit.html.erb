<% content_for :title, "Modifier la carte" %>

<div class="mx-auto mt-10 max-w-md px-4">
  <h1 class="mb-6 text-2xl font-semibold">Modifier la carte</h1>
  <%= render "form", card: @card, url: loyalty_card_path(@card) %>
  <p class="mt-6 text-sm"><%= link_to "← Fidélité", loyalty_cards_path, class: "underline" %></p>
</div>
