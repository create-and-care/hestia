<% content_for :title, "Nouveau contact" %>

<div class="mx-auto mt-10 max-w-md px-4">
  <h1 class="mb-6 text-2xl font-semibold">Nouveau contact</h1>
  <%= render "form", contact: @contact, url: contacts_path %>
  <p class="mt-6 text-sm"><%= link_to "← Anniversaires", contacts_path, class: "underline" %></p>
</div>
