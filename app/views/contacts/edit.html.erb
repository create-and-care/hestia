<% content_for :title, "Modifier le contact" %>

<div class="mx-auto mt-10 max-w-md px-4">
  <h1 class="mb-6 text-2xl font-semibold">Modifier le contact</h1>
  <%= render "form", contact: @contact, url: contact_path(@contact) %>
  <p class="mt-6 text-sm"><%= link_to "← Anniversaires", contacts_path, class: "underline" %></p>
</div>
