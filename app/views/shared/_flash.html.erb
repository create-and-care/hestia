<% flash.each do |type, message| %>
  <% next if message.blank? %>
  <%= render(Ui::AlertComponent.new(variant: type.to_s == "alert" ? :destructive : :success, class_name: "mb-4")) { message } %>
<% end %>
