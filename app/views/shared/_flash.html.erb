<% %i[notice alert].each do |type| %>
  <% message = flash[type] %>
  <% next if message.blank? %>
  <div data-controller="flash" data-flash-message-value="<%= message %>"
       data-flash-variant-value="<%= type == :alert ? "destructive" : "success" %>"></div>
<% end %>
