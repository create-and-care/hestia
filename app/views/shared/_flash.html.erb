<% flash.each do |type, message| %>
  <% next if message.blank? %>
  <div data-controller="flash" data-flash-message-value="<%= message %>"
       data-flash-variant-value="<%= type.to_s == "alert" ? "destructive" : "success" %>"></div>
<% end %>
