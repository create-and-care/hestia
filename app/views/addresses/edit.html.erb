<% content_for :title, "Modifier l'adresse" %>

<div class="mx-auto mt-10 max-w-xl px-4">
  <h1 class="mb-6 text-2xl font-semibold">Modifier l'adresse</h1>
  <%= render "form", address: @address, url: address_path(@address) %>
  <p class="mt-6 text-sm"><%= link_to "← Adresses", addresses_path, class: "underline" %></p>
</div>
