<% content_for :title, "Nouvelle adresse" %>

<div class="mx-auto mt-10 max-w-xl px-4">
  <h1 class="mb-6 text-2xl font-semibold">Nouvelle adresse</h1>
  <%= render "form", address: @address, url: addresses_path %>
  <p class="mt-6 text-sm"><%= link_to "← Adresses", addresses_path, class: "underline" %></p>
</div>
