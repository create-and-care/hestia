<% content_for :title, t(".title") %>

<div class="mx-auto mt-16 max-w-md px-4">
  <h1 class="mb-6 text-2xl font-semibold"><%= t(".heading") %></h1>

  <%= render "shared/flash" %>

  <%= form_with model: @user, url: registration_path do |form| %>
    <% if @user.errors.any? %>
      <div class="mb-4 rounded-md border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">
        <ul class="list-inside list-disc">
          <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="space-y-4">
      <div>
        <%= form.label :name, t(".name"), class: "mb-1 block text-sm font-medium" %>
        <%= form.text_field :name, required: true, autofocus: true, autocomplete: "name",
              class: "w-full rounded-md border border-gray-300 px-3 py-2" %>
      </div>

      <div>
        <%= form.label :email_address, t(".email"), class: "mb-1 block text-sm font-medium" %>
        <%= form.email_field :email_address, required: true, autocomplete: "username",
              class: "w-full rounded-md border border-gray-300 px-3 py-2" %>
      </div>

      <div>
        <%= form.label :password, t(".password"), class: "mb-1 block text-sm font-medium" %>
        <%= form.password_field :password, required: true, autocomplete: "new-password", maxlength: 72,
              class: "w-full rounded-md border border-gray-300 px-3 py-2" %>
      </div>

      <div>
        <%= form.label :password_confirmation, t(".password_confirmation"), class: "mb-1 block text-sm font-medium" %>
        <%= form.password_field :password_confirmation, required: true, autocomplete: "new-password", maxlength: 72,
              class: "w-full rounded-md border border-gray-300 px-3 py-2" %>
      </div>

      <%= form.submit t(".submit"), class: "w-full cursor-pointer rounded-md bg-gray-900 px-4 py-2 font-medium text-white hover:bg-gray-700" %>
    </div>
  <% end %>

  <p class="mt-6 text-sm text-gray-600">
    <%= t(".already_have_account") %> <%= link_to t(".sign_in"), new_session_path, class: "font-medium text-gray-900 underline" %>
  </p>
</div>
