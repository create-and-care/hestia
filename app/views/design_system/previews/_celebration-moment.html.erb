<div class="w-full max-w-md">
  <%= render(Ui::CelebrationMomentComponent.new(kind: :birthday, title: "L'anniversaire de Camille approche", note: "Dans 3 jours")) do |c| %>
    <% c.with_action { render(Ui::ButtonComponent.new(variant: :ghost, size: :sm)) { "Voir" } } %>
  <% end %>
</div>
