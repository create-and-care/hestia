<% content_for :title, "Modifier le véhicule" %>

<div class="mx-auto mt-10 max-w-xl px-4">
  <h1 class="mb-6 text-2xl font-semibold">Modifier le véhicule</h1>
  <%= render "form", vehicle: @vehicle, url: vehicle_path(@vehicle) %>
  <p class="mt-6 text-sm"><%= link_to "← Fiche", vehicle_path(@vehicle), class: "underline" %></p>
</div>
