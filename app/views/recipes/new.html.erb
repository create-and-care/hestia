<% content_for :title, "Nouvelle recette" %>

<div class="mx-auto mt-10 max-w-2xl px-4">
  <h1 class="mb-6 text-2xl font-semibold">Nouvelle recette</h1>
  <%= render "form", recipe: @recipe, url: recipes_path %>
  <p class="mt-6 text-sm"><%= link_to "← Recettes", recipes_path, class: "underline" %></p>
</div>
