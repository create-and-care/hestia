<div id="<%= dom_id(entry, :catalog) %>" class="rounded-lg border border-gray-200 p-4">
  <h2 class="font-medium"><%= entry.title %></h2>
  <div class="mt-1 flex flex-wrap items-center gap-2 text-xs text-gray-500">
    <% entry.tags.each do |tag| %>
      <span class="rounded-full bg-gray-100 px-2 py-0.5">#<%= t("recipe_catalog.tags.#{tag}", default: tag.humanize) %></span>
    <% end %>
    <% if entry.total_time_minutes %>
      <span>⏱ <%= t("recipes.recipe.total_time", minutes: entry.total_time_minutes) %></span>
    <% end %>
    <% if entry.servings %>
      <span>🍽 <%= entry.servings %></span>
    <% end %>
  </div>
  <%= button_to t(".add_link"), add_to_household_recipe_catalog_path(entry), method: :post,
        class: "mt-3 cursor-pointer rounded-md border border-gray-300 px-3 py-1.5 text-sm hover:bg-gray-50" %>
</div>
