<% content_for :title, t(".title") %>

<div class="mx-auto mt-10 max-w-lg px-4">
  <div class="mb-4"><%= module_breadcrumb(:wellbeing, [ t("workout_templates.index.title"), workout_templates_path ], [ t(".title"), nil ], module_path: wellbeing_path) %></div>

  <h1 class="mb-6 text-2xl font-semibold"><%= t(".title") %></h1>

  <%= render "form", template: @template, url: workout_templates_path %>
</div>
