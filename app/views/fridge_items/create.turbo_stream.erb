<%# Le produit est inséré via le flux temps réel ; ici on réinitialise le formulaire. %>
<%= turbo_stream.replace "new_fridge_item" do %>
  <%= render "fridge_items/form", fridge_item: FridgeItem.new %>
<% end %>
