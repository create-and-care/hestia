<% content_for :title, t(".title") %>

<div class="mt-10 max-w-5xl mx-auto px-4">
  <header class="mb-8">
    <%= render(Ui::GreetingHeaderComponent.new(name: Current.user.name.presence || Current.user.email_address)) %>
  </header>

  <%= render "shared/flash" %>

  <div class="mb-8">
    <%= render(Ui::HouseholdHeaderComponent.new(name: @household.name, note: t(".members", count: @memberships.size), members: dashboard_member_avatars(@memberships))) do |c| %>
      <% c.with_action do %>
        <div class="flex items-center gap-2">
          <code class="rounded-md bg-surface-inset px-3 py-1 text-sm font-semibold tracking-widest"><%= @household.invite_code %></code>
          <%= render(Ui::CopyButtonComponent.new(value: @household.invite_code, label: t(".copy_code"))) %>
        </div>
      <% end %>
    <% end %>
  </div>

  <% if @vehicles_needing_attention.any? %>
    <section class="mb-8">
      <h2 class="mb-3 flex items-center gap-1.5 font-medium text-destructive"><%= render Ui::ModuleMedallionComponent.new(mod: :vehicles, icon: "triangle-alert", size: :sm) %> <%= t(".vehicles_needing_attention") %></h2>
      <div class="divide-y divide-primary rounded-lg border border-primary">
        <% @vehicles_needing_attention.each do |vehicle| %>
          <%= render Ui::ItemComponent.new do |item| %>
            <% item.with_title { link_to vehicle.name, vehicle_path(vehicle), class: "hover:underline" } %>
            <% item.with_description { t(".inspection_expires_on", date: l(vehicle.inspection_expires_on, format: :short)) } %>
            <% item.with_trailing { render(Ui::BadgeComponent.new(variant: inspection_badge_variant(vehicle.inspection_status))) { inspection_label(vehicle.inspection_status) } } %>
          <% end %>
        <% end %>
      </div>
    </section>
  <% end %>

  <% if @plants_needing_attention&.any? %>
    <section class="mb-8">
      <h2 class="mb-3 flex items-center gap-1.5 font-medium text-destructive"><%= render Ui::ModuleMedallionComponent.new(icon: "sprout", size: :sm) %> <%= t(".plants_needing_attention") %></h2>
      <div class="divide-y divide-primary rounded-lg border border-primary">
        <% @plants_needing_attention.each do |plant| %>
          <%= render Ui::ItemComponent.new do |item| %>
            <% item.with_title { link_to plant.name, plant_path(plant), class: "hover:underline" } %>
            <% item.with_trailing { render(Ui::BadgeComponent.new(variant: care_status_badge_variant(plant.care_status))) { care_status_label(plant.care_status) } } %>
          <% end %>
        <% end %>
      </div>
    </section>
  <% end %>

  <% if @overdue_tasks&.any? %>
    <section class="mb-8">
      <h2 class="mb-3 flex items-center gap-1.5 font-medium text-destructive"><%= render Ui::ModuleMedallionComponent.new(mod: :tasks, icon: "square-check", size: :sm) %> <%= t(".overdue_tasks") %></h2>
      <div class="divide-y divide-primary rounded-lg border border-primary">
        <% @overdue_tasks.each do |task| %>
          <%= render Ui::ItemComponent.new do |item| %>
            <% item.with_title { link_to task.title, tasks_path, class: "hover:underline" } %>
            <% item.with_description { l(task.due_on, format: :short) } %>
            <% item.with_trailing { render(Ui::BadgeComponent.new(variant: due_badge_variant(:overdue))) { due_label(:overdue) } } %>
          <% end %>
        <% end %>
      </div>
    </section>
  <% end %>

  <% if @fridge_items_expiring&.any? %>
    <section class="mb-8">
      <h2 class="mb-3 flex items-center gap-1.5 font-medium"><%= render Ui::ModuleMedallionComponent.new(mod: :fridge, icon: "refrigerator", size: :sm) %> <%= t(".fridge_expiring") %></h2>
      <div class="divide-y divide-primary rounded-lg border border-primary">
        <% @fridge_items_expiring.each do |item| %>
          <%= render Ui::ItemComponent.new do |i| %>
            <% i.with_title { link_to item.name, fridge_path, class: "hover:underline" } %>
            <% i.with_description { l(item.expires_on, format: :short) } %>
            <% i.with_trailing { render(Ui::BadgeComponent.new(variant: expiration_badge_variant(item.expiration_status))) { expiration_label(item.expiration_status) } } %>
          <% end %>
        <% end %>
      </div>
    </section>
  <% end %>

  <% if @upcoming_birthdays&.any? %>
    <% next_birthday_contact = @upcoming_birthdays.first %>
    <section class="mb-8">
      <%= render(Ui::CelebrationMomentComponent.new(kind: :birthday, title: t(".upcoming_birthday_title", name: next_birthday_contact.name),
            note: l(next_birthday_contact.next_birthday, format: :short))) do |c| %>
        <% c.with_action { render(Ui::ButtonComponent.new(variant: :ghost, size: :sm, href: contacts_path)) { t(".upcoming_birthdays") } } %>
      <% end %>
      <% if @upcoming_birthdays.size > 1 %>
        <div class="mt-3 divide-y divide-primary rounded-lg border border-primary">
          <% @upcoming_birthdays.drop(1).each do |contact| %>
            <%= render Ui::ItemComponent.new do |item| %>
              <% item.with_title { link_to contact.name, contacts_path, class: "hover:underline" } %>
              <% item.with_description { l(contact.next_birthday, format: :short) } %>
              <% item.with_trailing { render(Ui::BadgeComponent.new(variant: proximity_badge_variant(contact.proximity_status))) { proximity_label(contact.proximity_status) } } %>
            <% end %>
          <% end %>
        </div>
      <% end %>
    </section>
  <% end %>

  <% if @upcoming_events&.any? %>
    <section class="mb-8">
      <h2 class="mb-3 flex items-center gap-1.5 font-medium"><%= render Ui::ModuleMedallionComponent.new(mod: :calendar, icon: "calendar", size: :sm) %> <%= t(".upcoming_events") %></h2>
      <div class="divide-y divide-primary rounded-lg border border-primary">
        <% @upcoming_events.each do |time, event| %>
          <%= render Ui::ItemComponent.new do |item| %>
            <% item.with_title { link_to event.title, calendar_path, class: "hover:underline" } %>
            <% item.with_description { l(time, format: :weekday_numeric) } %>
          <% end %>
        <% end %>
      </div>
    </section>
  <% end %>

  <% if @recipe_suggestions&.any? %>
    <section class="mb-8">
      <h2 class="mb-3 flex items-center gap-1.5 font-medium"><%= render Ui::ModuleMedallionComponent.new(mod: :recipes, icon: "chef-hat", size: :sm) %> <%= t(".recipe_suggestions") %></h2>
      <p class="mb-2 text-sm text-secondary"><%= t(".recipe_suggestions_description") %></p>
      <div class="divide-y divide-primary rounded-lg border border-primary">
        <% @recipe_suggestions.each do |recipe| %>
          <%= render Ui::ItemComponent.new(href: recipe_path(recipe)) do |item| %>
            <% item.with_title { recipe.title } %>
          <% end %>
        <% end %>
      </div>
    </section>
  <% end %>

  <% if @other_households.any? %>
    <section class="mb-8">
      <h2 class="mb-3 font-medium"><%= t(".switch_household") %></h2>
      <ul class="space-y-2">
        <% @other_households.each do |household| %>
          <li>
            <%= render(Ui::ButtonToComponent.new(activate_household_path(household), method: :patch, variant: :outline, size: :sm)) { t(".switch_to", name: household.name) } %>
          </li>
        <% end %>
      </ul>
    </section>
  <% end %>

  <section class="flex gap-3">
    <%= render(Ui::ButtonComponent.new(variant: :outline, size: :sm, href: new_household_path)) { t(".create_household") } %>
    <%= render(Ui::ButtonComponent.new(variant: :outline, size: :sm, href: new_membership_path)) { t(".join_household") } %>
  </section>
</div>
