<%= render(Ui::DialogComponent.new(position: :bottom)) do |dialog| %>
  <% dialog.with_trigger { trigger } %>
  <% dialog.with_title { title } if title? %>
  <% dialog.with_description { description } if description? %>
  <% dialog.with_footer { footer } if footer? %>
  <%= content %>
<% end %>
