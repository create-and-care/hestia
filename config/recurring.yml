# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

production:
  clear_solid_queue_finished_jobs:
    command: "SolidQueue::Job.clear_finished_in_batches(sleep_between_batches: 0.3)"
    schedule: every hour at minute 12

  # One-off Task/Calendar reminders: delivered once due.
  deliver_due_reminders:
    class: Reminders::DeliverDueJob
    queue: default
    schedule: every 5 minutes

  # Recurring Fridge/Birthday notifications.
  daily_notifications_digest:
    class: Reminders::DailyDigestJob
    queue: default
    schedule: every day at 7:00am

  # External calendar sync: pulls events from every active
  # Google/Microsoft/CalDAV connection into the local calendar.
  external_calendar_sync:
    class: Calendar::ExternalSync::SyncAllJob
    queue: default
    schedule: every 30 minutes
